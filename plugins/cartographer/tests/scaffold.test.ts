// plugins/cartographer/tests/scaffold.test.ts
// sanity check that the vitest toolchain is wired up correctly

import { describe, expect, it } from 'vitest';

describe('scaffold', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
